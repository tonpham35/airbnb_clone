<%= form_with(model: reservation, local: true) do |form| %>
  <% if reservation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reservation.errors.count, "error") %> prohibited this reservation from being saved:</h2>

      <ul>
      <% reservation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :reservation_user_id %>
  </div>

  <div class="field">
    <%= form.label :listing_id %>
    <%= form.text_field :listing_id, id: :reservation_listing_id %>
  </div>

  <div class="field">
    <%= form.label :total_price %>
    <%= form.number_field :total_price, id: :reservation_total_price %>
  </div>

  <div class="field">
    <%= form.label :currency %>
    <%= form.text_field :currency, id: :reservation_currency %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date, id: :reservation_start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date, id: :reservation_end_date %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :reservation_message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
