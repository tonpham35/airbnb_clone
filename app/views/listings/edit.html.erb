<% x = Listing.find(params[:id]) %>
<% if current_user == x.user %>

<h1 style="padding-top: 120px;">Editing Listing</h1>


<%= render 'form', listing: @listing %>


<%= link_to 'Show', @listing %> |
<%= link_to 'Back', listings_path %>

<% else %>
redirect_to root_path
<% end %>
